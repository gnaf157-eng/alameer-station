<resources>
    <!-- ثيم داكن دائم (لا يتأثر بوضع النظام) بدون شريط عنوان أعلى الشاشة -->
    <style name="Theme.Misbaha" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/solid_bg</item>
        <item name="android:statusBarColor">@color/bg_bottom</item>
        <item name="android:navigationBarColor">@color/bar_bg</item>
        <item name="android:colorPrimary">@color/green_primary</item>
        <item name="android:colorPrimaryDark">@color/bg_bottom</item>
        <item name="android:colorAccent">@color/green_primary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
