plugins { id 'com.android.application' }

android {
    namespace 'com.alameer.station.shifts'
    compileSdk 35
    defaultConfig {
        applicationId 'com.alameer.station.shifts'
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName '1.1.0'
        buildConfigField 'String', 'UPDATE_MANIFEST_URL', '""'
        buildConfigField 'String', 'SYNC_URL', '""'
        buildConfigField 'String', 'SYNC_SECRET', '""'
    }
    buildFeatures { buildConfig true }
}
