# Security

- لا تُرفع ملفات التوقيع (`.jks` أو `.keystore`) إلى المستودع.
- لا تُحفظ روابط سرية أو رموز وصول داخل الكود.
- تُوقّع إصدارات الإنتاج من بيئة بناء محمية فقط.
- يجب تغيير رموز PIN التجريبية قبل التشغيل الفعلي.
- يُبلّغ عن المشكلات الأمنية للمالك بصورة خاصة، وليس عبر Issue عام.
