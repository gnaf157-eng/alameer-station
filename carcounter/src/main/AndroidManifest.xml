<?xml version="1.0" encoding="utf-8"?>
<!--
  تطبيق "عداد السيارات"
  ملاحظة أمنية مقصودة: لا يوجد في هذا التطبيق إذن الإنترنت (android.permission.INTERNET)
  ولا أي إذن خاص بالتسجيل أو التخزين. لذلك لا يمكن للتطبيق إرسال الفيديو أو الصور
  إلى أي خادم، ولا حفظ تسجيلات. كل المعالجة (الكشف، التتبع، العدّ) تجري داخل الهاتف.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarCounter">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
